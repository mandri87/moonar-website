# Resend (https://resend.com) — used by /api/contact to send "Request a Demo" emails.
RESEND_API_KEY=

# Optional: verified sender address in your Resend account.
# Falls back to Resend's shared onboarding address if not set.
CONTACT_FROM_EMAIL="Moonar Website <onboarding@resend.dev>"
